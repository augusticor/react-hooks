export const todoReducer = (state = [], action) => {
	switch (action.type) {
		// case 'create':
		// 	console.log('Create');
		// 	break;
		// case 'update':
		// 	console.log('Update');
		// 	break;
		default:
			return state;
	}
};
